import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hyman.wordgame',
  appName: 'WordGame',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
